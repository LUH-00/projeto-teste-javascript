function freteGratis(valor){
    if (valor > 0 ) {
        return valor >=150 
    } else {
        return valor
    }
    
}

module.exports = freteGratis // Exportando a funcção com um módulo do node.js